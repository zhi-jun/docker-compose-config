## 安装docker-compose
```
pip --version
yum -y install epel-release
yum -y install python-pip
pip install docker-compose
```

## 创建并启动容器
```
docker-compose up -d
```

## 关闭并删除容器
```
docker-compose down
```
